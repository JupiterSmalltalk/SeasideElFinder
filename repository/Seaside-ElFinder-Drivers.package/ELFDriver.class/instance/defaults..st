options - permissions
defaults: anAssociationArray
  "defaults

Default file/directory permissions. Setting hidden, locked here - take no effect

Data type: array
Default value: 'defaults' => array('read' => true, 'write' => true)"

  ^ options at: #'defaults' put: anAssociationArray