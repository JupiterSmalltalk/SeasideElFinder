options - mime
mimefile: aString
  "mimefile

Path to alternative mime types file. Only used when mimeDetect set to internal. If not set will use default mime.types

Data type: string
Default value: ''"

  ^ options at: #'mimefile' put: aString